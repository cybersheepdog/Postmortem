# bec_hunt - dependencies
# ============================================================================
# The core analyzer runs on the Python standard library alone (Python 3.10+).
# Scanning .eml files, ingesting MBOX, exporting HTML/JSON/CSV/IOC reports, and
# parsing a Microsoft 365 Unified Audit Log all work with NO third-party
# packages installed.
#
# Everything below is optional. Uncomment what you need.

# --- Optional: Outlook PST/OST ingestion ------------------------------------
# Enables reading .pst / .ost containers directly. Without it, PST/OST inputs
# are skipped with a message pointing you at `readpst`. MBOX needs nothing.
# libpff-python>=20231205

# --- Optional: development / testing ----------------------------------------
# Run the test suite (`pytest -q`) and the lint sweep (`pyflakes`).
# pytest>=7.0
# pyflakes>=3.0
